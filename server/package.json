{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start":  "json-server --watch data/data.json --port 5000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "dnazmije",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
